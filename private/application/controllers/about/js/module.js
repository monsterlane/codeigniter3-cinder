
define( [ 'system/js/module', 'system/js/jquery.min' ], function( aModule ) {
	'use strict';

	/*
	===============================================================================
	Class: About
	===============================================================================
	*/

	var Module = aModule.subClass({
		/**
		 * Method: init
		 */

		init: function( aData ) {
			this._super( aData );

		},


	});

	return Module;
});
